<% include ../partials/header %>

<h1>Berichten:</h1>

<% product.messages.forEach(function(message){ %>
<p><%= message.author.username %> - <%=moment(message.createdAt).format("DD MMM, h:mm")%> - <%= message.content %></p>
<a class="btn btn-primary" href="/advertenties/<%=product_id%>/messages/<%=message.id%>/reactie">Reageer</a>
<% }) %>

<br />

<% include ../partials/footer %>